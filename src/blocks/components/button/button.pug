mixin button(tag, text, view, modal)
    if tag === 'a'
      a.button(href='/', class=`${view ? `button__${view}` : ''}`) #{text}

    else
      #{tag}.button(data-modal=modal)(class=`${view ? `button__${view}` : ''}`) #{text}
