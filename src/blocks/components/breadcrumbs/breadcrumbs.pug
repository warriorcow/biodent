mixin breadcrumbs(items, theme)
    .breadcrumbs(class=`${theme ? `breadcrumbs--${theme}` : ''}`)
        each item in items
            a.breadcrumbs__item(href=item.link, class=item.active ? 'active' : '')= item.name
